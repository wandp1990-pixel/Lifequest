# 배틀 시스템 — 남은 작업

2026-05-16 전투 시스템 검증 사이클의 미적용 항목 정리.

**적용·배포 완료** (git log 보존):
- HIGH 11건 + 핫픽스 1건 (last_regen_at 누락 3곳, restoreMode=half 이중 회복, ItemsTab HP 손실, schema.ts 세미콜론 등) — `4f87c3b`, `3b3a0c8`, `9b44d4a`, `015c7d0`
- `computeEffectiveStats` helper 추출 + 5곳 통일, skill-db/battle-config auth fail-closed 통일 — `e69f90d`
- `character.max_hp` schema 주석, 옵션 파서 dev warning, `applyItemPassives` `max_skp` cap, `/api/skills` PUT 트랜잭션화
- `/api/character` DELETE `mode=full` 능력치 리셋 (옵션 A)
- `getPassivePool` → `getGachaPassiveSource` rename + `item_passive_pool` dead seed 제거 (SoT = `skill_table`)
- ADMIN_SECRET UI 헤더 — SettingsDrawer admin 토큰 입력란 + `lib/admin/token.ts` helper + SkillDbPanel/BattleConfigPanel 적용
- `runBattle` 분해 + `trigger_condition` 상수화 (2026-05-16)
  - `lib/skills/triggers.ts` 신규 — `SKILL_TRIGGERS` 상수. `battle.ts` 의 8곳 trigger_condition 문자열 비교를 모두 상수 참조로 교체. DB 값(`skill_table.trigger_condition`) 은 그대로 유지하여 마이그레이션 회피
  - `runBattle` 분해 — `runPlayerTurn`/`runMonsterTurn`/`applyHpBelow25Heal`/`applyEvery3TurnsBonus`/`applyOnCritBonus`/`applyOnHitExtra`/`applyOnDeathSurvive`/`applyOnEvadeCounter`/`computeBattleStartPatkBonus`/`pushTurnLog` 헬퍼로 추출. `runBattle` 본체는 setup + 상위 loop + buildResult 만 보유
  - 회귀 fixture 마련: `verification/battle-fixtures/capture.ts` — mulberry32 seeded RNG 로 16 시나리오 deterministic 캡처. baseline ↔ after JSON diff = 0. JSON 산출물은 `.gitignore` 처리, 스크립트만 SoT 로 유지

**사용자 후속 1회 작업** (배포만으로 안 됨):
- `npx vercel env add ADMIN_SECRET production` — admin 토큰 값 등록
- 배포 후 LifeQuest 앱 → Settings → 관리자 섹션에서 같은 값 입력 (브라우저별 1회)
- 등록 전까지 SkillDb / BattleConfig 패널의 PUT/POST/DELETE 는 503 으로 차단

---

## 처리 우선순위 (남은 항목)

(없음 — 후순위 LOW 항목만 남음)

---

## 후순위 (LOW, 필요 시점에)

- `gainExp` 트랜잭션 내 equipment read 동시성 — 단일 사용자 환경 안전. 멀티유저 확장 시 재검토
- `calcRegen` KST 하드코딩 — 다국가 확장 시점에
- `parseEquippedStatBonuses` 와 `buildPlayerCombatStats` 옵션 파싱 코드 중복 — 공통 파서 추출
- `SkillDetailSheet` 의 `SURVIVE` 등 `base_value` 의미없는 스킬도 "5.0%" 표시 — 효과 표시 룰 보강
- gacha 옵션 단위 (`%` vs `Pt`) 처리 명시 주석
- `item_slot_table.excluded` 의 dead 항목 "물리방어력" 제거 또는 ability_pool 매칭 추가
- `unit="HP"` / `unit="MP"` ability 의 `base_value` 가 `rollAbilityValue` 에서 무시되고 grade.stat_min/max 만 적용 — 의도 미구현인지 확인
- `monster_table` DROP — schema 에 dead 주석만 있는 상태, 검증 후 안전하게 제거 가능
- `item_passive_pool` 테이블 자체 DROP — 시드는 정리됐지만 빈 테이블 잔존. 마이그레이션 SQL (5번/9번) 의 UPDATE 도 의미 없음. prod DB 정리 시 함께 처리
